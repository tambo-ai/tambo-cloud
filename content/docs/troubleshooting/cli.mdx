---
title: CLI
description: How to use the Hydra AI CLI
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";

# CLI Troubleshooting Guide

This guide covers common issues you might encounter when using the Hydra AI CLI and how to resolve them.

## Common Issues

### Installation Problems

#### CLI Not Found

If you get a "command not found" error after installation:

<Tabs items={["Error", "Solutions"]}>

```bash tab="Error"
hydra: command not found
```

```bash tab="Solutions"
# 1. Reinstall globally
npm install --global hydra-ai-cli

# 2. Check PATH
echo $PATH

# 3. Use npx instead
npx hydra-ai-cli
```

</Tabs>

### Authentication Issues

#### Login and Signup

<Tabs items={["Error", "New Users", "Existing Users"]}>

```bash tab="Error"
Error: Failed to log in
```

```bash tab="New Users"
# Create a new account
hydra signup
# or
npx hydra-ai-cli signup
```

```bash tab="Existing Users"
# Login to existing account
hydra login
# or
npx hydra-ai-cli login
```

</Tabs>

### Project Management

#### Listing Projects

If you can't see your projects:

```bash title="List projects"
hydra listProjects
# or
npx hydra-ai-cli listProjects
```

The command will show a table with project IDs and names. If it fails:

1. Verify you're logged in
2. Check your internet connection
3. Try logging out and back in

#### Creating New Projects

To create a new project:

```bash title="Create a new project"
hydra createProject
# or
npx hydra-ai-cli createProject
```

You'll be prompted to enter a project name. If creation fails:

1. Ensure you have a valid login session
2. Try using a different project name
3. Check for network connectivity

#### Adding Provider Keys

To add an OpenAI API key to your project:

```bash title="Add an OpenAI API key"
hydra addProvider --id <project_id>
# or
npx hydra-ai-cli addProvider --id <project_id>
```

You'll be prompted to enter your OpenAI API key securely.

## Debug Mode

<Tabs items={["Debug Logging", "Sample Output"]}>

```bash tab="Debug Logging"
# Enable detailed logging
hydra --debug <command>
# or
npx hydra-ai-cli --debug <command>
```

```bash tab="Sample Output"
[DEBUG] Initializing CLI...
[DEBUG] Checking authentication...
[DEBUG] Loading configuration from ~/.hydra/config.json
[DEBUG] Making API request to https://api.hydra.ai/v1/...
```

</Tabs>

## Version Conflicts

If you experience version-related issues:

1. Check your CLI version:

```bash title="Check CLI version"
hydra --version
# or
npx hydra-ai-cli --version
```

2. Update to the latest version:

```bash title="Update CLI"
npm update --global hydra-ai-cli
# or
npm update --global @hydra-ai/cli
```

## Network Problems

### Timeout Issues

For slow connections, increase the timeout:

```bash title="Increase timeout"
hydra <command> --timeout 30000
# or
npx hydra-ai-cli <command> --timeout 30000
```

## Getting Help

If you're still experiencing issues:

1. Run the help command:

```bash title="Run help command"
hydra help
# or
npx hydra-ai-cli help
```

2. Check the verbose logs:

```bash title="Check verbose logs"
hydra <command> --verbose
# or
npx hydra-ai-cli <command> --verbose
```

import { MailIcon, MessageCircleIcon } from "lucide-react";

<Cards>
  <Card
    icon={<MailIcon />}
    href="mailto:support@usehydra.ai"
    title="Email Support"
  >
    Contact us at support@usehydra.ai
  </Card>
  <Card
    icon={<MessageCircleIcon />}
    href="https://discord.gg/AkCvQ3sM"
    title="Discord Community"
  >
    Join our Discord community for help
  </Card>
</Cards>
