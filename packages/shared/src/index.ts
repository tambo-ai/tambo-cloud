// Re-export shared constants (and any future shared utilities) from the
// package root so that consumers can simply do:
//
//   import { DEFAULT_OPENAI_MODEL } from "@tambo-ai-cloud/shared";
//
export { DEFAULT_OPENAI_MODEL } from "./constants";
