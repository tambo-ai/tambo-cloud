# Inkeep API Configuration
# Required for Q&A and documentation search functionality
INKEEP_API_KEY=your_inkeep_api_key_here

# Optional: Custom Inkeep API base URL (defaults to https://api.inkeep.com/v1)
# INKEEP_API_BASE_URL=https://api.inkeep.com/v1

# Database Configuration
# Required for anonymous MCP usage logging
DATABASE_URL=your_database_connection_string_here

# Sentry Error Monitoring (Optional)
# For production error tracking and monitoring
# SENTRY_DSN=your_sentry_dsn_here
# NEXT_PUBLIC_SENTRY_DSN=your_public_sentry_dsn_here

# Sentry Environment Configuration (Optional)
# SENTRY_ENVIRONMENT=production
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# Development Notes:
# - The server runs without analytics/error logging when the relevant env vars are not set
# - INKEEP_API_KEY is required for the MCP tools to function
# - DATABASE_URL is needed for usage tracking but failures are ignored in edge runtime
# - All Sentry variables are optional and used only when provided