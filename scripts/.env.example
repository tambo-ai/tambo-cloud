# API URL (Optional - defaults to http://localhost:3001)
HYDRA_API_URL=http://localhost:3001

# API Key (Required)
HYDRA_API_KEY=your-api-key

# GitHub API token (optional) - Used for higher rate limits when fetching stars
GITHUB_TOKEN=your_github_personal_access_token_here

